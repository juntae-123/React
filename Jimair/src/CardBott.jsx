const CardBott = (props) => {
    const  {chpa, repa, freecaa ,freeasd} = props
    return(
        <div>
            <span>{chpa}</span>
            <span>{repa}</span>
            <span>{freecaa}</span>
            <span>{freeasd}</span>
        </div>
    )
}
export default CardBott